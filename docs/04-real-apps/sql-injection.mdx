---
sidebar_position: 3
title: SQL injection and safe queries
sidebar_label: SQL injection
description: The most common database vulnerability - how string-built SQL lets attackers in, and how parameterized queries stop it cold.
---

import SqlRunner from '@site/src/components/SqlRunner';
import Quiz from '@site/src/components/Quiz';

# SQL injection and safe queries

The most common and most damaging database vulnerability has one cause: **building SQL by gluing user input into a query string**. When input becomes part of the SQL text, an attacker can rewrite your query. The fix is simple and absolute - and you should adopt it as a reflex.

## The attack

Say a login looks up a user by name, and the app builds the query by concatenation:

```js
// DANGEROUS - never do this
const sql = "SELECT * FROM users WHERE name = '" + input + "'";
```

For a normal name it works. But an attacker types `' OR '1'='1` as the name. The string becomes `... WHERE name = '' OR '1'='1'` - always true - and every row comes back. Run it:

<SqlRunner
  query={`CREATE TABLE users (id INTEGER, name TEXT, token TEXT);
INSERT INTO users VALUES (1, 'ana', 'tok_a'), (2, 'ben', 'tok_b');

-- the app meant: WHERE name = '<input>'
-- the attacker's input was:  ' OR '1'='1
SELECT * FROM users WHERE name = '' OR '1'='1';`}
  height={180}
/>

Every user and token leaked. Worse inputs can read other tables, or with stacked statements like `'; DROP TABLE users; --` destroy data. The query *did exactly what the text said* - the problem is that the attacker got to write part of the text.

## The fix: parameterized queries

Never assemble SQL from input. Use **parameterized queries** (also called prepared statements): write placeholders, and pass the values **separately**. The driver sends the SQL and the data on different channels, so input is only ever treated as a *value*, never as SQL to parse.

```js
// safe - the driver binds `input` as a value
db.query('SELECT * FROM users WHERE name = $1', [input]);
```

```python
# safe - placeholders, values passed separately
cur.execute("SELECT * FROM users WHERE name = %s", (input,))
```

Now `' OR '1'='1` is searched for *literally* as a name - it matches nothing, exactly as intended. **ORMs and query builders parameterize by default**, which is a big part of why they improve safety. There is no performance reason to concatenate; parameterized queries are also faster to reuse.

## Defense in depth

Parameterizing is the main fix. Layer more behind it:

- **Least privilege.** If the app's role can only `SELECT` what it needs, an injection that slips through can do far less. (See [DCL](./dcl.mdx).)
- **Validate and allowlist.** You **cannot** parameterize a table or column name - placeholders are for values only. When SQL must include a dynamic identifier (a sort column, say), check it against an **allowlist** of known-good names; never interpolate raw input.
- **Avoid dynamic SQL** where you can; keep generated SQL minimal and reviewed.

The rule in 2026 is unchanged because the fix has never changed: **parameterize every query that touches user input.** One extra note - SQL generated by an LLM or copied from the web deserves the same scrutiny; review it for concatenated input before it ships.

## Quick quiz

<Quiz
  title="SQL injection"
  questions={[
    {
      prompt: "What is the root cause of SQL injection?",
      options: [
        {text: "Building SQL by concatenating user input into the query string", correct: true},
        {text: "Using too many indexes", correct: false},
        {text: "Running queries inside a transaction", correct: false},
        {text: "Normalizing the schema", correct: false},
      ],
      explanation: "When input becomes part of the SQL text, an attacker can change what the query does. That is the whole vulnerability.",
    },
    {
      prompt: "How do parameterized queries prevent injection?",
      options: [
        {text: "SQL and values travel separately, so input is treated only as a value, never as SQL", correct: true},
        {text: "They encrypt the user input", correct: false},
        {text: "They make the query run faster", correct: false},
        {text: "They escape the database from the network", correct: false},
      ],
      explanation: "Placeholders bind values out-of-band; the database never parses the input as SQL, so `' OR '1'='1` is just a literal search string.",
    },
    {
      prompt: "You need to let users sort by a column they pick. Why can't you just parameterize the column name?",
      options: [
        {text: "Placeholders bind values, not identifiers - validate the name against an allowlist instead", correct: true},
        {text: "You can - column names are values", correct: false},
        {text: "Sorting can never be user-controlled", correct: false},
        {text: "You must concatenate it directly", correct: false},
      ],
      explanation: "Parameters are for values only. For a dynamic table/column name, check it against a fixed allowlist of permitted identifiers.",
    },
    {
      prompt: "Which extra layer limits the damage if an injection does get through?",
      options: [
        {text: "Least-privilege database roles (DCL)", correct: true},
        {text: "A larger connection pool", correct: false},
        {text: "More indexes", correct: false},
        {text: "A higher isolation level", correct: false},
      ],
      explanation: "If the app's role can only do what it needs, a successful injection is far more contained - defense in depth on top of parameterizing.",
    },
  ]}
/>

:::tip Next up
**[ORMs and migrations](./orms-migrations.mdx)** - how application code talks to the database, and how the schema evolves safely over time.
:::
